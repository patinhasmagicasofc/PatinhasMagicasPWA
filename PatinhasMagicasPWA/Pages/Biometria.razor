@page "/perfil/biometria"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/perfil/configuracoes", replace: true);
    }
}
